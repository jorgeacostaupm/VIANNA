/* import { computeCorrelationMatrixData } from '@/components/VAPUtils/correlation_functions';

onmessage = function (event) {
  const [data, columns, groups, groupVar] = event.data;
  const result = computeCorrelationMatrixData(data, columns, groups, groupVar);
  console.log('result', result);
  postMessage(result);
}; */
